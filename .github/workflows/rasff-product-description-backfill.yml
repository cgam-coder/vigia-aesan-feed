name: VIGIA RASFF staged backfill completion

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/rasff-product-description-backfill.yml"

permissions:
  contents: read

concurrency:
  group: vigia-rasff-controlled-activation
  cancel-in-progress: true

jobs:
  stopped:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve checkpoint after persistent official RASFF 5xx
        run: echo "RASFF writes remain stopped at the persisted checkpoint."
