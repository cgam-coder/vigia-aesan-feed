name: VIGIA RASFF extraordinary carrier (stopped)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stopped:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm neutralized workflow
        run: echo "This extraordinary RASFF writer is intentionally neutralized."
